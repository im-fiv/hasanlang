func print_hello_world() -> void do
	print("hello, world!");
end

print_hello_world();

func random_number<Type>() -> Type do
	return 5;
end

func arguments(arg1: int, arg2: int, arg3: int) -> int do
	return arg1 + arg2 + arg3;
end

func no_return_type(a: int) do
	return a;
end

func main() do
	var add = 2;
	return random_number<int>() + add;
end

// NOTE: Function declarations have been removed (they are yet to come back later)
// func function_declaration<A, B>(first: A, second: this) -> B!;